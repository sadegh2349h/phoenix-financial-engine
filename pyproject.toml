[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "phoenix-financial-engine"
version = "0.1.0"
description = "PHOENIX modular intelligence ecosystem core"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
